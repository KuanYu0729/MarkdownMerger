{
	"name": "vscode-markdown-to-pdf",
	"displayName": "Markdown → PDF (embed images & internal links)",
	"description": "Convert Markdown to PDF with embedded images (base64) and internal links that scroll to headings.",
	"version": "0.0.1",
	"engines": {
		"vscode": "^1.60.0"
	},
	"main": "./out/extension.js",
	"scripts": {
		"vscode:prepublish": "npm run compile",
		"compile": "tsc -p ./",
		"watch": "tsc -w -p ./",
		"pretest": "npm run compile",
		"test": "node ./out/test/runTest.js"
	},
	"devDependencies": {
		"typescript": "^4.5.4",
		"@types/node": "^16.11.7",
		"@types/vscode": "^1.60.0",
		"vscode-test": "^1.6.2"
	},
	"dependencies": {
		"markdown-it": "^13.0.1",
		"markdown-it-anchor": "^8.6.6",
		"puppeteer": "^21.0.0",
		"jsdom": "^22.0.0",
		"mime": "^3.0.0"
	},
	"contributes": {
		"commands": [
			{
				"command": "markdownToPdf.convert",
				"title": "Markdown: Export to PDF (embed images)"
			}
		]
	}
}